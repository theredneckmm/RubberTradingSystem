@inject NavigationManager NavManager

@code {
    protected override void OnInitialized()
    {
        // GitHub Pages အပါအဝင် application ရဲ့ BaseUri ကို အသုံးပြုပြီး Login သို့သွားမည်
        NavManager.NavigateTo(
            NavManager.BaseUri + "login",
            true
        );
    }
}
